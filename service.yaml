apiVersion: v1
kind: Service
metadata:
  name: my-compte-service
spec:
  type: NodePort
  selector:
    app: compte-service
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30007
      protocol: TCP
